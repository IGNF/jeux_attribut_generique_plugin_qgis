name: Release QGIS Plugin

on:
  push:
    tags:
      - '*'

jobs:
  build-release:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install zip
        run: sudo apt-get install zip -y

      - name: Build plugin zip
        run: |
          mkdir -p IGN_jeux_attributs_generique
          rsync -av --progress . IGN_jeux_attributs_generique/ \
            --exclude .git \
            --exclude .github \
            --exclude IGN_jeux_attributs_generique
          zip -r IGN_jeux_attributs_generique.zip IGN_jeux_attributs_generique

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: "IGN_jeux_attributs_generique.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}